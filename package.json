{
  "name": "kaza-analiz-paneli",
  "version": "1.0.0",
  "private": true,
  "description": "Kolluk kuvvetleri için trafik kazası analiz ve risk noktası paneli",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm start -w server",
    "test": "npm test -w server"
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
