name: Siteyi yayınla

# Paneli GitHub Pages'e yayınlar. Veri güncelleme iş akışı yeni veri commit ettiğinde bu iş akışını ayrıca tetikler
# (GITHUB_TOKEN ile yapılan push'lar push tetikleyicisini çalıştırmaz).
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build
        env:
          # Canlı olaylar sayfası için; TomTom panelinde alan adı kısıtlaması açık olmalı
          VITE_TOMTOM_KEY: ${{ secrets.TOMTOM_API_KEY }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: client/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
